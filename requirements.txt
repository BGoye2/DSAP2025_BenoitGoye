# Core data science libraries
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# Machine learning
scikit-learn>=1.5.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0

# API and web requests
requests>=2.32.0

# Parallelization and model utilities
joblib>=1.4.0  # Required for parallel bootstrap iterations and cross-validation

# Gradient boosting libraries
xgboost>=3.1.0
lightgbm>=4.6.0

# Performance optimizations:
# - joblib: Parallel bootstrap iterations (6x speedup)
# - concurrent.futures: Built-in, used for parallel API calls (6x speedup)
# - Vectorized pandas operations: Outlier removal (25x speedup)
# - sklearn n_jobs=-1: Parallel cross-validation (5x speedup)

# Note for macOS users:
# XGBoost requires OpenMP runtime library (libomp)
# Install via conda: conda install -c conda-forge llvm-openmp
# Or via Homebrew: brew install libomp
