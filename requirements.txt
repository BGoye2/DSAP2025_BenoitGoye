# GINI Coefficient Prediction - Dependencies
# Python 3.8+ required (3.12+ recommended for best performance)

# Core data science libraries
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# Machine learning
scikit-learn>=1.5.0
joblib>=1.4.0  # Parallel processing for bootstrap and CV

# Gradient boosting libraries
xgboost>=3.1.0
lightgbm>=4.6.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0

# API requests
requests>=2.32.0

# Note: concurrent.futures (used for parallel API calls) is built-in to Python 3.2+
# Note: threading (used for spinner animation) is built-in to Python

# Performance features enabled:
# - Parallel API calls via concurrent.futures (6x speedup)
# - Parallel bootstrap iterations via joblib (6x speedup)
# - Vectorized pandas operations (25x speedup)
# - Parallel cross-validation via sklearn (5x speedup)

# macOS users: XGBoost requires OpenMP
# Install via: brew install libomp
# Or via conda: conda install -c conda-forge llvm-openmp
